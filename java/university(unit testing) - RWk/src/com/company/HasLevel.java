package com.company;

public interface HasLevel {
    /**
     * 1. Create a new interface, HasLevel.
     * This should define a method getLevel() which returns an instance of the Level enum.
     * Have your Student implement the HasLevel interface.
     * The students level should be entered as a constructor parameter and maintained as a private field.
     *
     */

}
